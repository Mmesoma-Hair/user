# Storefront public assets

Drop the test store's real assets here (they're referenced by the UI):

- `logo.png`  — store logo (shown in the header; falls back to a wordmark if missing)
- `hero.jpg`  — homepage hero image (falls back to a brand gradient if missing)

These are git-ignored binaries on purpose — keep brand assets out of the repo
or add them intentionally.
